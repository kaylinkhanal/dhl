import React from 'react'

const Nav = (props)=>{
    return(
        <>
            <div className="card">
              <button>Logout</button>
            </div>
        </>
    )
}
export default Nav